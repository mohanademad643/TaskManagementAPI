
public enum TaskStatus
{
    Todo = 1,
    InProgress = 2,
    Done = 3,
    Cancelled = 4
}

public enum TaskPriority
{
    Low = 1,
    Medium = 2,
    High = 3,
    Critical = 4
}

public enum UserRole
{
    Admin = 1,
    User = 2
}
